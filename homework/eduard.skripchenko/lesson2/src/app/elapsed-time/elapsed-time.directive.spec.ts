import { ElapsedTimeDirective } from './elapsed-time.directive';

describe('ElapsedTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new ElapsedTimeDirective();
    expect(directive).toBeTruthy();
  });
});
