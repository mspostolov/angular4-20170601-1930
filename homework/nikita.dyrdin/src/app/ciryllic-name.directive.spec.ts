import { CiryllicNameDirective } from './ciryllic-name.directive';

describe('CiryllicNameDirective', () => {
  it('should create an instance', () => {
    const directive = new CiryllicNameDirective();
    expect(directive).toBeTruthy();
  });
});
