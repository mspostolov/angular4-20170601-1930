import { CountryfromISOPipe } from './countryfrom-iso.pipe';

describe('CountryfromISOPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryfromISOPipe();
    expect(pipe).toBeTruthy();
  });
});
