import { ReceivingDateFormatPipe } from './receiving-date-format.pipe';

describe('ReceivingDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ReceivingDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
